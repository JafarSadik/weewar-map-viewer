#!/bin/bash

# dos2unix - converts files with DOC/MAC/WIN line breaks to Unix line breaks
sudo apt-get install --yes --force-yes dos2unix

# httpie - http client, a better alternative to curl or wget
sudo apt-get install --yes --force-yes httpie
